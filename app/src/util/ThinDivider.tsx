import React from 'react';

const ThinDivider = () => {
  // const style = `container h-2 bg-${props.color}`;
  //this keeps breaking so I'm hard coding the color for now.

  return <div className="container h-0.5 bg-dgsoftwhite" />;
};

export default ThinDivider;
